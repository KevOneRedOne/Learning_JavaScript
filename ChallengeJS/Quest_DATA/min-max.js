// max() takes 2 number as argumentsand returns the greatest
const max = (num1, num2) => (num1 > num2 ? num1 : num2);
// min() takes 2 number as argumentsand returns the lowest
const min = (num1, num2) => (num1 < num2 ? num1 : num2);


// console.log(min(1,2))
// console.log(max(5,2))
